apiVersion: v1
kind: Service
metadata:
  name: react-cicd-demo
  namespace: dev
  labels:
    app: react-cicd-demo
spec:
  selector:
    app: react-cicd-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: LoadBalancer